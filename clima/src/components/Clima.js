import React, { Fragment } from 'react';

const Clima = ({ clima }) => {

    console.log(clima);

    const { air_quality, results } = clima;

    return (
        <Fragment>

            <h1> </h1>

        </Fragment>
    );
}

export default Clima;